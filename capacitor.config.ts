import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.solid.sample',
  appName: 'solid-capacitor-ts',
  webDir: 'build/',
  bundledWebRuntime: false
};

export default config;